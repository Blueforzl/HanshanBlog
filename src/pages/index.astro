---
const title = "Conor Liu - Full-Stack Developer";
const description = "Building intelligent systems with code, design, and AI.";
---

<html lang="zh">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&family=Bebas+Neue&display=swap" rel="stylesheet" />
  </head>
  <body>

    <div id="cursor"></div>
    <div id="cursor-follower"></div>

    <canvas id="shader-canvas"></canvas>
    <canvas id="particle-canvas"></canvas>

    <nav class="navbar">
      <div class="nav-container">
        <a href="/" class="logo magnetic">CL</a>
        <ul class="nav-links">
          <li><a href="/" class="nav-link active">Home</a></li>
          <li><a href="/articles" class="nav-link">Blog</a></li>
          <li><a href="/projects" class="nav-link">Projects</a></li>
          <li><a href="/lab" class="nav-link">Lab</a></li>
          <li><a href="/about" class="nav-link">About</a></li>
        </ul>
        <div class="nav-status">
          <span class="status-dot"></span>
          <span class="status-text">Available</span>
        </div>
        <button class="menu-toggle" aria-label="Toggle menu">
          <span></span>
          <span></span>
          <span></span>
        </button>
      </div>
    </nav>

    <section id="hero">
      <div class="hero-content">
        <div class="hero-badge">Full-Stack Developer / AI Engineer</div>
        <h1 class="hero-title">
          <span class="hero-line">Building</span>
          <span class="hero-line">
            <span class="hero-gradient">intelligent</span> systems
          </span>
          <span class="hero-line">with code & creativity</span>
        </h1>
        <p class="hero-description">
          I design and build scalable full-stack applications, create interactive web experiences, 
          and explore the intersection of AI and engineering. Currently focused on AI infrastructure, 
          multi-agent systems, and creative frontend development.
        </p>
        <div class="hero-cta">
          <a href="/articles" class="btn btn-primary magnetic">Read Blog</a>
          <a href="/projects" class="btn btn-outline magnetic">View Projects</a>
        </div>
        <div class="hero-tags">
          <span class="tag">React</span>
          <span class="tag">TypeScript</span>
          <span class="tag">Node.js</span>
          <span class="tag">WebGL</span>
          <span class="tag">AI</span>
        </div>
      </div>
      <div class="scroll-hint">
        <div class="scroll-line"></div>
        <span>Scroll</span>
      </div>
    </section>

    <div class="ticker-wrapper">
      <div class="ticker">
        <span class="ticker-item">React</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">Next.js</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">TypeScript</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">Node.js</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">WebGL</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">GLSL</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">GSAP</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">Three.js</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">AI</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">PostgreSQL</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">Docker</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">Vercel</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">React</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">Next.js</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">TypeScript</span>
        <span class="ticker-separator">·</span>
        <span class="ticker-item">Node.js</span>
      </div>
    </div>

    <section id="featured">
      <div class="section-header">
        <span class="section-label">Featured</span>
        <h2 class="section-title">Latest <span class="gradient-text">Writing</span></h2>
      </div>
      <div class="featured-grid">
        <article class="featured-card featured-large reveal">
          <div class="card-thumbnail">
            <canvas class="thumb-canvas" data-hue="220" data-speed="0.8"></canvas>
          </div>
          <div class="card-content">
            <span class="card-category">AI Engineering</span>
            <h3 class="card-title">AI Development Standards: Building Stable AI Systems in Production</h3>
            <p class="card-excerpt">A five-layer constraint system for making AI work reliably in real projects: AGENTS.md, documentation infrastructure, Cursor Rules, Feature Gate, and Spike-First methodology.</p>
            <div class="card-meta">
              <span class="card-date">April 28, 2026</span>
              <span class="card-read">Read →</span>
            </div>
          </div>
        </article>
        <article class="featured-card reveal">
          <div class="card-thumbnail">
            <canvas class="thumb-canvas" data-hue="160" data-speed="1.2"></canvas>
          </div>
          <div class="card-content">
            <span class="card-category">Open Source</span>
            <h3 class="card-title">ai-memory: Turn Cursor Conversations into Knowledge Base</h3>
            <p class="card-excerpt">Extract decisions, architecture patterns, and conventions directly from AI editor chat logs.</p>
            <div class="card-meta">
              <span class="card-date">April 28, 2026</span>
            </div>
          </div>
        </article>
        <article class="featured-card reveal">
          <div class="card-thumbnail">
            <canvas class="thumb-canvas" data-hue="280" data-speed="1.0"></canvas>
          </div>
          <div class="card-content">
            <span class="card-category">Architecture</span>
            <h3 class="card-title">Designing a 47KB AI Gateway</h3>
            <p class="card-excerpt">Six subsystems: priority routing, conditional degradation, budget control, unified SSE streaming, error classification, and exponential backoff retry.</p>
            <div class="card-meta">
              <span class="card-date">April 2, 2026</span>
            </div>
          </div>
        </article>
      </div>
      <div class="section-footer">
        <a href="/articles" class="view-all">View All Articles →</a>
      </div>
    </section>

    <section id="projects-preview">
      <div class="section-header">
        <span class="section-label">Selected Work</span>
        <h2 class="section-title">Projects</h2>
      </div>
      <div class="projects-bento">
        <div class="bento-item bento-large reveal">
          <div class="bento-content">
            <span class="bento-tag">AI Pipeline</span>
            <h3 class="bento-title">ai-review-pipeline</h3>
            <p class="bento-desc">AI-powered code quality pipeline with zero dependencies, supporting 6 major AI providers.</p>
            <div class="bento-tech">
              <span>TypeScript</span>
              <span>AI</span>
              <span>CLI</span>
            </div>
          </div>
          <div class="bento-glow"></div>
        </div>
        <div class="bento-item reveal">
          <span class="bento-tag">Gateway</span>
          <h3 class="bento-title">ai-gateway-lite</h3>
          <p class="bento-desc">Lightweight AI gateway with multi-model routing and budget control.</p>
        </div>
        <div class="bento-item reveal">
          <span class="bento-tag">Git Tooling</span>
          <h3 class="bento-title">ai-git-msg</h3>
          <p class="bento-desc">AI-powered commit message generator.</p>
        </div>
        <div class="bento-item reveal">
          <span class="bento-tag">i18n</span>
          <h3 class="bento-title">ai-i18n-sync</h3>
          <p class="bento-desc">AI translation and incremental sync for internationalization.</p>
        </div>
        <div class="bento-item reveal">
          <span class="bento-tag">Memory</span>
          <h3 class="bento-title">ai-memory</h3>
          <p class="bento-desc">Structured knowledge extraction from AI conversations.</p>
        </div>
      </div>
      <div class="section-footer">
        <a href="/projects" class="view-all">View All Projects →</a>
      </div>
    </section>

    <section id="lab-preview">
      <div class="section-header">
        <span class="section-label">Motion Lab</span>
        <h2 class="section-title">Interactive <span class="gradient-text">Experiments</span></h2>
      </div>
      <div class="lab-grid">
        <div class="lab-card reveal">
          <canvas class="lab-canvas" data-type="flow"></canvas>
          <div class="lab-info">
            <span class="lab-tag">Shader</span>
            <h3 class="lab-title">Fluid Gradient</h3>
            <p class="lab-desc">Real-time WebGL fluid simulation</p>
          </div>
        </div>
        <div class="lab-card reveal">
          <canvas class="lab-canvas" data-type="particles"></canvas>
          <div class="lab-info">
            <span class="lab-tag">Particles</span>
            <h3 class="lab-title">Particle System</h3>
            <p class="lab-desc">Interactive particle simulation</p>
          </div>
        </div>
        <div class="lab-card reveal">
          <canvas class="lab-canvas" data-type="noise"></canvas>
          <div class="lab-info">
            <span class="lab-tag">Noise</span>
            <h3 class="lab-title">Procedural Noise</h3>
            <p class="lab-desc">Smooth noise generation</p>
          </div>
        </div>
      </div>
      <div class="section-footer">
        <a href="/lab" class="view-all">Explore Lab →</a>
      </div>
    </section>

    <section id="about-preview">
      <div class="about-content">
        <div class="about-text">
          <span class="section-label">About</span>
          <h2 class="section-title">Building <span class="gradient-text">digital experiences</span> with purpose</h2>
          <p class="about-desc">
            With 8+ years of experience, I've evolved from traditional frontend development to AI full-stack architecture. 
            I'm passionate about creating beautiful, performant, and intelligent web applications that push boundaries.
          </p>
          <div class="about-stats">
            <div class="stat-item">
              <span class="stat-value">7+</span>
              <span class="stat-label">Open Source Projects</span>
            </div>
            <div class="stat-item">
              <span class="stat-value">70+</span>
              <span class="stat-label">Technical Articles</span>
            </div>
            <div class="stat-item">
              <span class="stat-value">2.4K</span>
              <span class="stat-label">GitHub Stars</span>
            </div>
          </div>
        </div>
        <div class="about-image">
          <div class="image-container">
            <div class="image-glow"></div>
          </div>
        </div>
      </div>
    </section>

    <section id="contact">
      <div class="contact-content">
        <span class="section-label">Get in Touch</span>
        <h2 class="section-title">Let's <span class="gradient-text">build</span> something together</h2>
        <p class="contact-desc">
          Available for freelance projects, full-time opportunities, and collaborations. 
          Feel free to reach out for discussions about AI engineering, frontend development, or creative coding.
        </p>
        <div class="contact-links">
          <a href="mailto:conor@example.com" class="contact-link magnetic">
            <span class="link-icon">✉</span>
            <span class="link-text">conor@example.com</span>
          </a>
          <a href="https://github.com/conorliu" target="_blank" class="contact-link">
            <span class="link-icon">☁</span>
            <span class="link-text">GitHub</span>
          </a>
          <a href="https://twitter.com/conorliu" target="_blank" class="contact-link">
            <span class="link-icon">↗</span>
            <span class="link-text">Twitter</span>
          </a>
        </div>
      </div>
    </section>

    <footer>
      <div class="footer-content">
        <span class="footer-text">© 2026 Conor Liu. Built with code & coffee.</span>
        <div class="footer-links">
          <a href="/">Home</a>
          <a href="/articles">Blog</a>
          <a href="/projects">Projects</a>
          <a href="/about">About</a>
        </div>
      </div>
    </footer>

  </body>
</html>

<style>
  *, *::before, *::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :root {
    --bg-primary: #050505;
    --bg-secondary: #0B0B0D;
    --bg-card: rgba(255, 255, 255, 0.03);
    --bg-card-hover: rgba(255, 255, 255, 0.05);
    --border: rgba(255, 255, 255, 0.08);
    --border-hover: rgba(255, 255, 255, 0.15);
    
    --text-primary: #F5F5F5;
    --text-secondary: #D4D4D8;
    --text-muted: #A1A1AA;
    --text-subtle: #71717A;
    
    --accent-blue: #60A5FA;
    --accent-violet: #8B5CF6;
    --accent-cyan: #22D3EE;
    --gradient: linear-gradient(135deg, #60A5FA, #8B5CF6, #22D3EE);
    --gradient-text: linear-gradient(135deg, #60A5FA, #8B5CF6);
    
    --font-sans: 'Inter', system-ui, -apple-system, sans-serif;
    --font-mono: 'JetBrains Mono', 'Space Mono', monospace;
    --font-display: 'Bebas Neue', sans-serif;
    
    --shadow: 0 4px 24px rgba(0, 0, 0, 0.3);
    --shadow-hover: 0 8px 32px rgba(96, 165, 250, 0.15);
    
    --radius-sm: 8px;
    --radius-md: 12px;
    --radius-lg: 16px;
    
    --transition-fast: 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    --transition-normal: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    --transition-slow: 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  html {
    scroll-behavior: smooth;
    cursor: none;
  }

  body {
    background-color: var(--bg-primary);
    color: var(--text-primary);
    font-family: var(--font-sans);
    line-height: 1.6;
    overflow-x: hidden;
    min-height: 100vh;
  }

  /* CURSOR */
  #cursor {
    position: fixed;
    width: 10px;
    height: 10px;
    background: var(--accent-blue);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9999;
    transform: translate(-50%, -50%);
    transition: width 0.2s, height 0.2s, background 0.2s;
    mix-blend-mode: difference;
  }

  #cursor-follower {
    position: fixed;
    width: 40px;
    height: 40px;
    border: 1.5px solid rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9998;
    transform: translate(-50%, -50%);
    transition: all 0.12s ease;
    mix-blend-mode: difference;
  }

  /* NOISE OVERLAY */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1000;
  }

  /* SHADER CANVAS */
  #shader-canvas {
    position: fixed;
    inset: 0;
    z-index: 0;
    opacity: 0.4;
  }

  #particle-canvas {
    position: fixed;
    inset: 0;
    z-index: 1;
    pointer-events: none;
  }

  /* NAVBAR */
  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 500;
    padding: 1rem 2rem;
    background: rgba(5, 5, 5, 0.65);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border);
    transition: all var(--transition-normal);
  }

  .nav-container {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    font-family: var(--font-display);
    font-size: 1.75rem;
    letter-spacing: 0.1em;
    background: var(--gradient-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-decoration: none;
    font-weight: 600;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    list-style: none;
  }

  .nav-link {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    text-decoration: none;
    letter-spacing: 0.05em;
    position: relative;
    transition: color var(--transition-fast);
  }

  .nav-link:hover,
  .nav-link.active {
    color: var(--text-primary);
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--accent-blue);
    transition: width var(--transition-normal);
  }

  .nav-link:hover::after,
  .nav-link.active::after {
    width: 100%;
  }

  .nav-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    color: var(--accent-cyan);
  }

  .status-dot {
    width: 6px;
    height: 6px;
    background: var(--accent-cyan);
    border-radius: 50%;
    animation: blink 2s infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  .menu-toggle {
    display: none;
    flex-direction: column;
    gap: 4px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
  }

  .menu-toggle span {
    width: 20px;
    height: 1.5px;
    background: var(--text-primary);
    transition: all var(--transition-normal);
  }

  /* HERO */
  #hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 6rem 2rem 4rem;
    position: relative;
    z-index: 10;
    text-align: center;
  }

  .hero-content {
    max-width: 900px;
    margin: 0 auto;
  }

  .hero-badge {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--accent-cyan);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    margin-bottom: 1.5rem;
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(34, 211, 238, 0.1);
    border-radius: 999px;
    border: 1px solid rgba(34, 211, 238, 0.2);
  }

  .hero-title {
    font-size: clamp(3rem, 8vw, 5.5rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .hero-line {
    display: block;
    opacity: 0;
    animation: fadeInUp 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  .hero-line:nth-child(1) { animation-delay: 0.2s; }
  .hero-line:nth-child(2) { animation-delay: 0.4s; }
  .hero-line:nth-child(3) { animation-delay: 0.6s; }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-gradient {
    background: var(--gradient-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-description {
    font-size: 1.125rem;
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 2rem;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2.5rem;
  }

  .btn {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 0.9rem 2rem;
    border-radius: var(--radius-md);
    text-decoration: none;
    transition: all var(--transition-normal);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .btn-primary {
    background: var(--gradient);
    color: white;
    border: none;
    box-shadow: 0 4px 20px rgba(96, 165, 250, 0.3);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(96, 165, 250, 0.4);
  }

  .btn-outline {
    background: transparent;
    color: var(--text-primary);
    border: 1px solid var(--border);
  }

  .btn-outline:hover {
    border-color: var(--accent-blue);
    color: var(--accent-blue);
    background: rgba(96, 165, 250, 0.05);
  }

  .hero-tags {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5rem;
  }

  .hero-tags .tag {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    padding: 0.4rem 0.8rem;
    background: rgba(96, 165, 250, 0.08);
    border: 1px solid rgba(96, 165, 250, 0.15);
    border-radius: 999px;
    color: var(--text-muted);
    opacity: 0;
    animation: fadeInUp 0.5s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  .hero-tags .tag:nth-child(1) { animation-delay: 0.8s; }
  .hero-tags .tag:nth-child(2) { animation-delay: 0.85s; }
  .hero-tags .tag:nth-child(3) { animation-delay: 0.9s; }
  .hero-tags .tag:nth-child(4) { animation-delay: 0.95s; }
  .hero-tags .tag:nth-child(5) { animation-delay: 1s; }

  .scroll-hint {
    position: absolute;
    bottom: 4rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    color: var(--text-subtle);
    letter-spacing: 0.1em;
    animation: bounce 2s infinite;
  }

  .scroll-line {
    width: 1px;
    height: 30px;
    background: var(--text-subtle);
    position: relative;
  }

  .scroll-line::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 4px;
    height: 4px;
    background: var(--accent-blue);
    border-radius: 50%;
    animation: scrollLine 1.5s infinite;
  }

  @keyframes scrollLine {
    0% { transform: translateX(-50%) translateY(0); opacity: 1; }
    100% { transform: translateX(-50%) translateY(-30px); opacity: 0; }
  }

  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(5px); }
  }

  /* TICKER */
  .ticker-wrapper {
    position: relative;
    z-index: 10;
    padding: 1.5rem 0;
    background: var(--bg-secondary);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    overflow: hidden;
  }

  .ticker {
    display: flex;
    animation: tickerScroll 60s linear infinite;
    white-space: nowrap;
  }

  .ticker:hover {
    animation-play-state: paused;
  }

  @keyframes tickerScroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .ticker-item {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    padding: 0 2rem;
    letter-spacing: 0.1em;
  }

  .ticker-separator {
    color: var(--border);
  }

  /* SECTIONS */
  section {
    position: relative;
    z-index: 10;
    padding: 6rem 2rem;
  }

  .section-header {
    margin-bottom: 3rem;
  }

  .section-label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--accent-blue);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    margin-bottom: 0.75rem;
    display: block;
  }

  .section-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1rem;
  }

  .gradient-text {
    background: var(--gradient-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section-footer {
    margin-top: 3rem;
    text-align: center;
  }

  .view-all {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--text-muted);
    text-decoration: none;
    letter-spacing: 0.1em;
    transition: color var(--transition-fast);
  }

  .view-all:hover {
    color: var(--accent-blue);
  }

  /* FEATURED ARTICLES */
  #featured {
    background: var(--bg-secondary);
  }

  .featured-grid {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 1.5rem;
  }

  .featured-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    transition: all var(--transition-normal);
    grid-column: span 6;
  }

  .featured-card:hover {
    border-color: var(--border-hover);
    transform: translateY(-4px);
    box-shadow: var(--shadow-hover);
  }

  .featured-large {
    grid-column: span 12;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
  }

  .card-thumbnail {
    position: relative;
    aspect-ratio: 16/9;
    overflow: hidden;
  }

  .featured-large .card-thumbnail {
    aspect-ratio: 4/3;
  }

  .thumb-canvas {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(96, 165, 250, 0.1), rgba(139, 92, 246, 0.1));
  }

  .card-content {
    padding: 1.5rem;
  }

  .featured-large .card-content {
    padding: 2rem 0;
  }

  .card-category {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    color: var(--accent-violet);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 0.75rem;
    display: inline-block;
    padding: 0.3rem 0.6rem;
    background: rgba(139, 92, 246, 0.1);
    border-radius: 4px;
  }

  .card-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    line-height: 1.4;
    transition: color var(--transition-fast);
  }

  .featured-large .card-title {
    font-size: 1.75rem;
  }

  .card-title:hover {
    color: var(--accent-blue);
  }

  .card-excerpt {
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.7;
    margin-bottom: 1rem;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .card-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-subtle);
  }

  .card-read {
    color: var(--accent-blue);
    transition: color var(--transition-fast);
  }

  .card-read:hover {
    color: var(--accent-cyan);
  }

  /* PROJECTS BENTO */
  #projects-preview {
    background: var(--bg-primary);
  }

  .projects-bento {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 1.5rem;
  }

  .bento-item {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    transition: all var(--transition-normal);
    position: relative;
    overflow: hidden;
  }

  .bento-item:hover {
    border-color: var(--border-hover);
    transform: translateY(-4px);
    box-shadow: var(--shadow-hover);
  }

  .bento-large {
    grid-column: span 7;
    grid-row: span 2;
    padding: 2rem;
  }

  .bento-item:nth-child(2) { grid-column: span 5; }
  .bento-item:nth-child(3) { grid-column: span 3; }
  .bento-item:nth-child(4) { grid-column: span 4; }
  .bento-item:nth-child(5) { grid-column: span 5; }

  .bento-glow {
    position: absolute;
    top: -50%;
    right: -50%;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(96, 165, 250, 0.1) 0%, transparent 70%);
    pointer-events: none;
  }

  .bento-tag {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    color: var(--accent-cyan);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 0.75rem;
    display: block;
  }

  .bento-title {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    transition: color var(--transition-fast);
  }

  .bento-item:hover .bento-title {
    color: var(--accent-blue);
  }

  .bento-desc {
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.6;
  }

  .bento-tech {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 1.5rem;
  }

  .bento-tech span {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    padding: 0.3rem 0.6rem;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 4px;
    color: var(--text-muted);
  }

  /* LAB PREVIEW */
  #lab-preview {
    background: var(--bg-secondary);
  }

  .lab-grid {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  .lab-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    transition: all var(--transition-normal);
  }

  .lab-card:hover {
    border-color: var(--border-hover);
    transform: translateY(-4px);
    box-shadow: var(--shadow-hover);
  }

  .lab-canvas {
    width: 100%;
    height: 180px;
    background: var(--bg-primary);
  }

  .lab-info {
    padding: 1.25rem;
  }

  .lab-tag {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    color: var(--accent-violet);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 0.5rem;
    display: block;
  }

  .lab-title {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.3rem;
  }

  .lab-desc {
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  /* ABOUT PREVIEW */
  #about-preview {
    background: var(--bg-primary);
  }

  .about-content {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .about-desc {
    color: var(--text-secondary);
    font-size: 1.05rem;
    line-height: 1.8;
    margin-bottom: 2.5rem;
  }

  .about-stats {
    display: flex;
    gap: 3rem;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }

  .stat-value {
    font-size: 2.25rem;
    font-weight: 700;
    background: var(--gradient-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .stat-label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-muted);
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .about-image {
    position: relative;
    aspect-ratio: 4/5;
  }

  .image-container {
    width: 100%;
    height: 100%;
    background: var(--bg-card);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    position: relative;
    overflow: hidden;
  }

  .image-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(96, 165, 250, 0.2) 0%, transparent 70%);
  }

  /* CONTACT */
  #contact {
    background: linear-gradient(135deg, var(--bg-secondary), var(--bg-primary));
    text-align: center;
  }

  .contact-content {
    max-width: 700px;
    margin: 0 auto;
  }

  .contact-desc {
    color: var(--text-secondary);
    font-size: 1.05rem;
    line-height: 1.8;
    margin-bottom: 2.5rem;
  }

  .contact-links {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1.5rem;
  }

  .contact-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--text-muted);
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    transition: all var(--transition-normal);
  }

  .contact-link:hover {
    border-color: var(--accent-blue);
    color: var(--accent-blue);
    background: rgba(96, 165, 250, 0.05);
  }

  .link-icon {
    font-size: 1rem;
  }

  /* FOOTER */
  footer {
    background: var(--bg-secondary);
    border-top: 1px solid var(--border);
    padding: 2.5rem 2rem;
  }

  .footer-content {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer-text {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-subtle);
    letter-spacing: 0.05em;
  }

  .footer-links {
    display: flex;
    gap: 2rem;
  }

  .footer-links a {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-subtle);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .footer-links a:hover {
    color: var(--text-primary);
  }

  /* MAGNETIC EFFECT */
  .magnetic {
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  /* REVEAL ANIMATION */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
    transition: opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1), transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* RESPONSIVE */
  @media (max-width: 1024px) {
    .featured-large {
      grid-column: span 12;
      grid-template-columns: 1fr;
    }
    .featured-card {
      grid-column: span 12;
    }
    .bento-large {
      grid-column: span 12;
    }
    .bento-item:nth-child(2) { grid-column: span 12; }
    .bento-item:nth-child(3) { grid-column: span 6; }
    .bento-item:nth-child(4) { grid-column: span 6; }
    .bento-item:nth-child(5) { grid-column: span 12; }
    .about-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .about-image {
      order: -1;
    }
  }

  @media (max-width: 768px) {
    .navbar {
      padding: 1rem;
    }
    .nav-links {
      display: none;
    }
    .nav-status {
      display: none;
    }
    .menu-toggle {
      display: flex;
    }
    #hero {
      padding: 4rem 1rem 2rem;
    }
    .hero-title {
      font-size: clamp(2.5rem, 7vw, 4rem);
    }
    .hero-description {
      font-size: 1rem;
    }
    .section-title {
      font-size: clamp(1.5rem, 4vw, 2.5rem);
    }
    .bento-item:nth-child(3),
    .bento-item:nth-child(4) {
      grid-column: span 12;
    }
    .about-stats {
      flex-wrap: wrap;
      gap: 1.5rem;
    }
    .footer-content {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
    }
  }
</style>

<script>
  // ─── SHADER BACKGROUND ───────────────────────────────────────────
  const sc = document.getElementById('shader-canvas');
  const sgl = sc.getContext('webgl') || sc.getContext('experimental-webgl');
  
  function resizeShader() {
    sc.width = innerWidth;
    sc.height = innerHeight;
    if (sgl) sgl.viewport(0, 0, sc.width, sc.height);
  }
  resizeShader();
  window.addEventListener('resize', resizeShader);

  if (sgl) {
    const gl = sgl;
    const vs = `attribute vec2 p;void main(){gl_Position=vec4(p,0,1);}`;
    const fs = `precision highp float;
uniform float t;uniform vec2 r;uniform vec2 m;
vec3 pal(float t,vec3 a,vec3 b,vec3 c,vec3 d){return a+b*cos(6.28318*(c*t+d));}
float n(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5);}
float sn(vec2 p){vec2 i=floor(p);vec2 f=fract(p);f=f*f*(3.-2.*f);return mix(mix(n(i),n(i+vec2(1,0)),f.x),mix(n(i+vec2(0,1)),n(i+vec2(1,1)),f.x),f.y);}
float fbm(vec2 p){float v=0.;float a=.5;for(int i=0;i<5;i++){v+=a*sn(p);p=p*2.1+vec2(1.7,9.2);a*=.5;}return v;}
void main(){
  vec2 uv=(gl_FragCoord.xy-.5*r)/min(r.x,r.y);
  vec2 mu=(m/r-.5)*2.;
  float time=t*.2;
  vec2 q=vec2(fbm(uv+vec2(0,0)),fbm(uv+vec2(5.2,1.3)));
  vec2 p2=uv+.5*q+vec2(1.7+sin(time*.5)*mu.x*.2,9.2+cos(time*.3)*mu.y*.2);
  float f=fbm(p2);
  vec3 col=pal(f+time*.05,vec3(.3,.3,.4),vec3(.2,.2,.3),vec3(.8,.8,1.),vec3(0,.1,.2));
  col=mix(col,vec3(.2,.1,.3),clamp(f*f*2.,0.,1.));
  col*=1.-length(uv)*.3;
  gl_FragColor=vec4(col*.25,1);
}`;

    function mkShader(gl, type, src) {
      const s = gl.createShader(type);
      gl.shaderSource(s, src);
      gl.compileShader(s);
      return s;
    }

    const prog = gl.createProgram();
    gl.attachShader(prog, mkShader(gl, gl.VERTEX_SHADER, vs));
    gl.attachShader(prog, mkShader(gl, gl.FRAGMENT_SHADER, fs));
    gl.linkProgram(prog);
    gl.useProgram(prog);

    const buf = gl.createBuffer();
    gl.bindBuffer(gl.ARRAY_BUFFER, buf);
    gl.bufferData(gl.ARRAY_BUFFER, new Float32Array([-1, -1, 1, -1, -1, 1, 1, 1]), gl.STATIC_DRAW);
    const loc = gl.getAttribLocation(prog, 'p');
    gl.enableVertexAttribArray(loc);
    gl.vertexAttribPointer(loc, 2, gl.FLOAT, false, 0, 0);

    const tl = gl.getUniformLocation(prog, 't');
    const rl = gl.getUniformLocation(prog, 'r');
    const ml = gl.getUniformLocation(prog, 'm');

    let mx = 0, my = 0;
    document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; });

    function animShader(ts) {
      gl.uniform1f(tl, ts * 0.001);
      gl.uniform2f(rl, sc.width, sc.height);
      gl.uniform2f(ml, mx, my);
      gl.drawArrays(gl.TRIANGLE_STRIP, 0, 4);
      requestAnimationFrame(animShader);
    }
    requestAnimationFrame(animShader);
  }

  // ─── PARTICLE SYSTEM ────────────────────────────────────────────
  const pc = document.getElementById('particle-canvas');
  const ctx = pc.getContext('2d');

  function resizePc() {
    pc.width = innerWidth;
    pc.height = innerHeight;
  }
  resizePc();
  window.addEventListener('resize', resizePc);

  const PARTICLE_COLORS = ['#60A5FA', '#8B5CF6', '#22D3EE'];
  const N = 60;
  const particles = Array.from({ length: N }, () => ({
    x: Math.random() * innerWidth,
    y: Math.random() * innerHeight,
    vx: (Math.random() - 0.5) * 0.2,
    vy: (Math.random() - 0.5) * 0.2,
    r: Math.random() * 1.5 + 0.5,
    c: PARTICLE_COLORS[Math.floor(Math.random() * PARTICLE_COLORS.length)],
    o: Math.random() * 0.3 + 0.1
  }));

  let pmx = 0, pmy = 0;
  document.addEventListener('mousemove', e => { pmx = e.clientX; pmy = e.clientY; });

  function animParticles() {
    ctx.clearRect(0, 0, pc.width, pc.height);
    particles.forEach(p => {
      const dx = pmx - p.x;
      const dy = pmy - p.y;
      const d = Math.sqrt(dx * dx + dy * dy);
      
      if (d < 120) {
        p.vx += (dx / d) * 0.03;
        p.vy += (dy / d) * 0.03;
      }
      
      p.vx *= 0.98;
      p.vy *= 0.98;
      p.x += p.vx;
      p.y += p.vy;

      if (p.x < 0) p.x = pc.width;
      if (p.x > pc.width) p.x = 0;
      if (p.y < 0) p.y = pc.height;
      if (p.y > pc.height) p.y = 0;

      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = p.c;
      ctx.globalAlpha = p.o;
      ctx.fill();
    });

    ctx.globalAlpha = 1;
    for (let i = 0; i < N; i++) {
      for (let j = i + 1; j < N; j++) {
        const dx = particles[i].x - particles[j].x;
        const dy = particles[i].y - particles[j].y;
        const d = Math.sqrt(dx * dx + dy * dy);
        
        if (d < 80) {
          ctx.beginPath();
          ctx.moveTo(particles[i].x, particles[i].y);
          ctx.lineTo(particles[j].x, particles[j].y);
          ctx.strokeStyle = particles[i].c;
          ctx.globalAlpha = (1 - d / 80) * 0.06;
          ctx.lineWidth = 0.5;
          ctx.stroke();
        }
      }
    }
    ctx.globalAlpha = 1;
    requestAnimationFrame(animParticles);
  }
  animParticles();

  // ─── CURSOR ANIMATION ───────────────────────────────────────────
  const cur = document.getElementById('cursor');
  const fol = document.getElementById('cursor-follower');
  let fx = 0, fy = 0, cx2 = 0, cy2 = 0;

  document.addEventListener('mousemove', e => {
    cx2 = e.clientX;
    cy2 = e.clientY;
    cur.style.left = cx2 + 'px';
    cur.style.top = cy2 + 'px';
  });

  function animCursor() {
    fx += (cx2 - fx) * 0.15;
    fy += (cy2 - fy) * 0.15;
    fol.style.left = fx + 'px';
    fol.style.top = fy + 'px';
    requestAnimationFrame(animCursor);
  }
  animCursor();

  document.querySelectorAll('a, button, .magnetic').forEach(el => {
    el.addEventListener('mouseenter', () => {
      cur.style.width = '24px';
      cur.style.height = '24px';
      cur.style.background = '#60A5FA';
      fol.style.transform = 'translate(-50%, -50%) scale(1.5)';
      fol.style.borderColor = 'rgba(96, 165, 250, 0.5)';
    });
    el.addEventListener('mouseleave', () => {
      cur.style.width = '10px';
      cur.style.height = '10px';
      cur.style.background = '#60A5FA';
      fol.style.transform = 'translate(-50%, -50%) scale(1)';
      fol.style.borderColor = 'rgba(255, 255, 255, 0.2)';
    });
  });

  // ─── MAGNETIC EFFECT ────────────────────────────────────────────
  document.querySelectorAll('.magnetic').forEach(el => {
    el.addEventListener('mousemove', e => {
      const rect = el.getBoundingClientRect();
      const x = e.clientX - rect.left - rect.width / 2;
      const y = e.clientY - rect.top - rect.height / 2;
      el.style.transform = `translate(${x * 0.3}px, ${y * 0.3}px)`;
    });
    el.addEventListener('mouseleave', () => {
      el.style.transform = 'translate(0, 0)';
    });
  });

  // ─── THUMBNAIL CANVASES ─────────────────────────────────────────
  document.querySelectorAll('.thumb-canvas').forEach(c => {
    const hue = +c.dataset.hue;
    const spd = +c.dataset.speed;
    c.width = c.offsetWidth;
    c.height = c.offsetHeight;
    const cx = c.getContext('2d');
    let t = 0;
    
    function drawThumb() {
      cx.clearRect(0, 0, c.width, c.height);
      for (let i = 0; i < 3; i++) {
        const x = c.width / 2 + Math.sin(t * spd + i * 1.5) * (c.width * 0.3);
        const y = c.height / 2 + Math.cos(t * spd * 0.8 + i * 1.2) * (c.height * 0.2);
        const r = c.width * 0.25 + Math.sin(t + i) * (c.width * 0.1);
        const gradient = cx.createRadialGradient(x, y, 0, x, y, r);
        gradient.addColorStop(0, `hsla(${hue + i * 30}, 70%, 60%, 0.25)`);
        gradient.addColorStop(1, `hsla(${hue + i * 30}, 70%, 60%, 0)`);
        cx.beginPath();
        cx.arc(x, y, r, 0, Math.PI * 2);
        cx.fillStyle = gradient;
        cx.fill();
      }
      t += 0.006;
      requestAnimationFrame(drawThumb);
    }
    drawThumb();
  });

  // ─── LAB CANVASES ───────────────────────────────────────────────
  document.querySelectorAll('.lab-canvas').forEach(c => {
    const type = c.dataset.type;
    c.width = c.offsetWidth;
    c.height = c.offsetHeight;
    const cx = c.getContext('2d');
    let t = 0;
    
    function drawLab() {
      cx.clearRect(0, 0, c.width, c.height);
      
      if (type === 'flow') {
        for (let i = 0; i < 5; i++) {
          const x = (c.width / 6) * (i + 1);
          const y = c.height / 2 + Math.sin(t * 0.5 + i * 0.8) * 30;
          const gradient = cx.createLinearGradient(x - 30, y, x + 30, y);
          gradient.addColorStop(0, 'rgba(96, 165, 250, 0)');
          gradient.addColorStop(0.5, 'rgba(96, 165, 250, 0.3)');
          gradient.addColorStop(1, 'rgba(96, 165, 250, 0)');
          cx.beginPath();
          cx.moveTo(x - 50, y);
          cx.quadraticCurveTo(x, y - 20, x, y);
          cx.quadraticCurveTo(x, y + 20, x + 50, y);
          cx.strokeStyle = gradient;
          cx.lineWidth = 2;
          cx.stroke();
        }
      } else if (type === 'particles') {
        for (let i = 0; i < 20; i++) {
          const x = (Math.sin(t * 0.3 + i * 0.5) + 1) * c.width / 2;
          const y = (Math.cos(t * 0.4 + i * 0.7) + 1) * c.height / 2;
          const r = 2 + Math.sin(t + i) * 1;
          cx.beginPath();
          cx.arc(x, y, r, 0, Math.PI * 2);
          cx.fillStyle = `rgba(139, 92, 246, ${0.3 + Math.sin(t + i) * 0.2})`;
          cx.fill();
        }
      } else if (type === 'noise') {
        for (let y = 0; y < c.height; y += 4) {
          cx.beginPath();
          cx.moveTo(0, y);
          for (let x = 0; x < c.width; x += 4) {
            const noise = Math.sin(x * 0.02 + t * 0.01) * Math.cos(y * 0.02 + t * 0.01);
            cx.lineTo(x, y + noise * 10);
          }
          cx.strokeStyle = 'rgba(34, 211, 238, 0.2)';
          cx.lineWidth = 1;
          cx.stroke();
        }
      }
      
      t += 0.016;
      requestAnimationFrame(drawLab);
    }
    drawLab();
  });

  // ─── SCROLL REVEAL ──────────────────────────────────────────────
  const io = new IntersectionObserver(entries => {
    entries.forEach((entry, index) => {
      if (entry.isIntersecting) {
        setTimeout(() => {
          entry.target.classList.add('visible');
        }, index * 100);
        io.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

  document.querySelectorAll('.reveal').forEach(el => io.observe(el));

  // ─── NAVIGATION ACTIVE STATE ────────────────────────────────────
  const currentPath = window.location.pathname;
  document.querySelectorAll('.nav-link').forEach(link => {
    if (link.getAttribute('href') === currentPath) {
      link.classList.add('active');
    }
  });
</script>